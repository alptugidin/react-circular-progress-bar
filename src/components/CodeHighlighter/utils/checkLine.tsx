import React from 'react';
export const checkLine = (line: string): JSX.Element => {
  const output = <span>{line}</span>;
  // if (line.includes('var ')) {
  // console.log(line);
  // }
  return output;
};
